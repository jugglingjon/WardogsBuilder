{
  "name": "wardogs-builder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Planning tool for designing constructions in the game Wardogs",
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "sass": "^1.80.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
